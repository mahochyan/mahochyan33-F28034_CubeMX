services:
  config-studio:
    build: .
    environment:
      APP_MODE: web
      PORT: 8080
    ports:
      - "8080:8080"
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true
